import { CopyLinkDirective } from './copy-link.directive';

describe('CopyLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyLinkDirective();
    expect(directive).toBeTruthy();
  });
});
