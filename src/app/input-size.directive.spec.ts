import { InputSizeDirective } from './input-size.directive';

describe('InputSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSizeDirective();
    expect(directive).toBeTruthy();
  });
});
